namespace pinch_payments_tech_test
{
    public enum Status
    {
        IDLE= 0,
        PENDING = 1,
        MOVING = 2,
    };

    public enum Direction
    {
        IDLE = 0,
        UP = 1,
        DOWN = 2,
    };
}
